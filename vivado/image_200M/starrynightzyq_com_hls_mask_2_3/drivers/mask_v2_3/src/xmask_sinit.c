// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xmask.h"

extern XMask_Config XMask_ConfigTable[];

XMask_Config *XMask_LookupConfig(u16 DeviceId) {
	XMask_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XMASK_NUM_INSTANCES; Index++) {
		if (XMask_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XMask_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XMask_Initialize(XMask *InstancePtr, u16 DeviceId) {
	XMask_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XMask_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XMask_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

