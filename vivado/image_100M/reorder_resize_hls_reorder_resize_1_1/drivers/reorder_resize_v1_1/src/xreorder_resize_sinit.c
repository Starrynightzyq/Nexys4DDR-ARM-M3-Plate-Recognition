// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2018.2
// Copyright (C) 1986-2018 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xreorder_resize.h"

extern XReorder_resize_Config XReorder_resize_ConfigTable[];

XReorder_resize_Config *XReorder_resize_LookupConfig(u16 DeviceId) {
	XReorder_resize_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XREORDER_RESIZE_NUM_INSTANCES; Index++) {
		if (XReorder_resize_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XReorder_resize_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XReorder_resize_Initialize(XReorder_resize *InstancePtr, u16 DeviceId) {
	XReorder_resize_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XReorder_resize_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XReorder_resize_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

